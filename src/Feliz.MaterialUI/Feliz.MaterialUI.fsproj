<Project Sdk="Microsoft.NET.Sdk">
  
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Description>Feliz-style Fable bindings for Material-UI.</Description>
    <PackageProjectUrl>https://github.com/cmeeren/Feliz.MaterialUI</PackageProjectUrl>
    <Authors>Christer van der Meeren</Authors>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageTags>f# fable bindings feliz react material-ui materialui</PackageTags>
    <Version>0.5.0</Version>
    <PackageReleaseNotes>- Disabled Feliz native DOM prop inheritance (seemingly broken due to F# compiler limitations, see #20 (comment)). Inheritance within MUI props are now based on simply duplicating inherited props for child components, and Feliz’s prop must be used for non-MUI props.
- Improved popover.anchorOrigin and popover.transformOrigin
- Added seq&lt;ReactElement&gt; overload to all components that support it</PackageReleaseNotes>
  </PropertyGroup>
  
  <ItemGroup>
    <Compile Include="ThemeOverrides.fs" />
    <Compile Include="ThemeProps.fs" />
    <Compile Include="Bindings.fs" />
    <Compile Include="Misc.fs" />
    <Compile Include="Hooks.fs" />
    <Compile Include="Colors.fs" />
    <Compile Include="Classes.fs" />
    <Compile Include="Styles.fs" />
    <Compile Include="Props.fs" />
    <Compile Include="Mui.fs" />
  </ItemGroup>
  
  <ItemGroup Condition="'$(Configuration)' == 'Release'">
    <Content Include="*.fsproj; *.fs" PackagePath="fable\" />
  </ItemGroup>
  
  <ItemGroup>
    <PackageReference Include="Feliz" Version="[0.57, 0.58)" />
  </ItemGroup>
  
  <PropertyGroup>
    <NpmDependencies>
      <NpmPackage Name="@material-ui/core" Version="gte 4.5 lt 5" ResolutionStrategy="max" />
      <NpmPackage Name="@material-ui/lab" Version="4.0.0-alpha.28" />
    </NpmDependencies>
  </PropertyGroup>
  
</Project>
